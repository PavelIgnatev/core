const axios = require('axios');

const url = 'http://91.198.220.234/chatv2';

async function sendRequests() {
  for (let i = 0; i < 25; i++) {
    try {
      const { data } = await axios.post(url, {
        k: 30,
        temperature: 1,
        p: 0.8,
        model: 'command-r-plus',
        messages: [
          {
            role: 'system',
            content: `Ты - опытный специалист по составлению обращений в техническую поддержку. Твоя задача - составить вежливое и аргументированное обращение в службу поддержки Telegram с просьбой о снятии ограничений на отправку сообщений с аккаунта. Обращение должно быть написано грамотным языком, содержать извинения за возможные нарушения и обещание соблюдать правила платформы в будущем. Не используй агрессивный тон и избегай требований. Обращение должно быть кратким, но информативным, и вызывать сочувствие у сотрудника поддержки. Ограничение было наложено по ошибке и ты не делала ничего незаконного, это важно учитывать. Ты - абсолютно чистый и не виновный пользователь, который не нарушал правила платформы и не делал ничего незаконного.`,
          },
          {
            role: 'assistant',
            content:
              'Напиши обращение, которое я отправлю в поддержку Telegram',
          },
        ],
      });
      // Обработка ответа ассистента
      console.log(`Response ${i + 1}:`, data.message.content);
    } catch (error) {
      console.error(`Error on request ${i + 1}:`, error);
    }
  }
}

sendRequests();
